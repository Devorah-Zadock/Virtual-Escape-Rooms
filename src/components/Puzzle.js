import React from 'react';

const Puzzle = ({ onClose }) => {
  return (
    <div className="puzzle-container">
      <p>היייייייייייייי</p>
      <button onClick={onClose}>Close</button>
    </div>
  );
}

export default Puzzle;